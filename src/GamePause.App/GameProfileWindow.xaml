<Window x:Class="GamePause.App.GameProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="游戏档案" Width="520" Height="610" MinWidth="500" MinHeight="590"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#EFF4F8" Foreground="#1E293B" FontFamily="Segoe UI" FontSize="14"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="38" />
            <Setter Property="MinWidth" Value="92" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.76" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" /><RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="游戏收藏与配置档案" FontSize="22" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="显示名称" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="DisplayNameBox" MaxLength="80" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,14,0,0">
            <TextBlock Text="进程" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="ProcessNameBox" IsReadOnly="True" Background="#E7EDF2" />
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <TextBlock Text="程序路径" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox x:Name="PathBox" IsReadOnly="True" Background="#E7EDF2" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
        </StackPanel>

        <CheckBox x:Name="DeepPauseCheck" Grid.Row="4" Content="使用深度暂停（冻结后回收物理工作集）" Margin="0,18,0,0" />
        <CheckBox x:Name="AutoPauseCheck" Grid.Row="5" Content="失去前台后自动暂停" Margin="0,14,0,0"
                  Checked="AutoPauseCheck_Changed" Unchecked="AutoPauseCheck_Changed" />
        <Grid Grid.Row="6" Margin="26,12,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="92" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <TextBlock Text="延迟" VerticalAlignment="Center" />
            <TextBox x:Name="DelayBox" Grid.Column="1" Margin="10,0" Text="10" />
            <TextBlock Grid.Column="2" Text="秒（3 - 300）" VerticalAlignment="Center" />
        </Grid>
        <StackPanel Grid.Row="7">
            <CheckBox x:Name="AutoResumeCheck" Content="回到该游戏前台时自动恢复" Margin="26,14,0,0" />
            <CheckBox x:Name="AllowCautionCheck" Content="允许“谨慎”项目执行自动规则（仅在确认安全模式后启用）"
                      Margin="26,14,0,0" />
            <TextBlock Text="自动规则默认关闭；“谨慎”项目必须单独确认，已阻止项目永远不会自动执行。"
                       Foreground="#5C6C7C" TextWrapping="Wrap" Margin="0,20,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" IsCancel="True" Margin="0,0,10,0" />
            <Button Content="保存" IsDefault="True" Background="#2678F6" Foreground="White" BorderThickness="0" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
